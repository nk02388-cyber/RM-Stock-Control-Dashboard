# QR Code สำหรับ RM Dashboard

เมนู **ผังพื้นที่คลัง** มีช่องสแกน QR และเมนู **พิมพ์ป้าย QR** ใช้สร้างป้ายตำแหน่งตามโซน หรือป้ายวัตถุดิบจากรหัสที่มีอยู่จริงในข้อมูล RM

- ป้ายตำแหน่ง: `RMLOC|โซน|รหัสตำแหน่ง` เช่น `RMLOC|A|A-01`
- ป้ายวัตถุดิบ: `RMITEM|รหัสวัตถุดิบ` เช่น `RMITEM|RM-AT-H-017`
- ป้ายพาเลตรับเข้า: `RMTAG|UUID` สร้างจากเมนู **รับเข้าและจัดเก็บ (QR)** (หนึ่งรหัสต่อหนึ่งพาเลต)

ใช้กล้องบนเว็บไซต์ HTTPS, เครื่องอ่าน QR ที่ส่งข้อความและ Enter หรือพิมพ์รหัสเองก็ได้ การสแกนป้ายตำแหน่งจะเปิดช่องในผัง; การสแกนป้ายวัตถุดิบจะค้นหาตำแหน่งที่บันทึกไว้ในเครื่องนี้ ป้าย `PKLOC`/`PKITEM` ของคลัง PK ไม่ใช้ปะปนกับ RM

การสแกนเป็นการค้นหาเท่านั้น **ไม่เพิ่ม เบิก หรือแก้ไขยอดคงเหลือ** การเปลี่ยนข้อมูลยังต้องตรวจและยืนยันในฟอร์มเดิม ข้อมูลผังและรายการในตำแหน่งเก็บในเบราว์เซอร์เครื่องนั้น ไม่ซิงค์ข้ามเครื่อง; ป้ายเดียวกันอาจแสดงรายการต่างกันหากแต่ละเครื่องมีข้อมูลไม่ตรงกัน

## รับเข้าและจัดเก็บแบบ PK WMS (ปรับใช้กับ RM)

1. เมนู **รับเข้าและจัดเก็บ (QR) → รับสินค้า**: ระบุเลขที่รับเข้า วันที่ ผู้รับ รหัสวัตถุดิบ/QR สินค้า Lot จำนวนรวม หน่วย และจำนวนพาเลต ระบบแบ่งยอดต่อพาเลตให้แก้ไขได้ โดยผลรวมต้องตรงกันก่อนบันทึก
2. พิมพ์ป้ายพาเลตแบบ FM-ST-019: A4 แนวนอน หนึ่งหน้าต่อหนึ่งพาเลต มีป้ายย่อยเหมือนกัน 4 ดวง และเลขลำดับ `1/2`, `2/2` ฯลฯ
3. **จัดเก็บ**: สแกนป้าย `RMTAG` แล้วสแกน `RMLOC` หรือเลือกโซน/ตำแหน่ง ระบุผู้จัดเก็บ ตรวจข้อมูล แล้วกด **ยืนยันจัดเก็บ** จึงเพิ่มรายการหนึ่งพาเลตลงผังคลัง ป้ายเดิมไม่สามารถยืนยันซ้ำได้
4. **รายการล่าสุด**: ดูป้ายรอจัดเก็บ/จัดเก็บแล้ว เลือกป้ายค้างเพื่อจัดเก็บ และพิมพ์ป้ายซ้ำ

ทะเบียนรับเข้าและผังคลังเป็นข้อมูล **เฉพาะเบราว์เซอร์เครื่องนี้** ไม่ได้ใช้ฐานข้อมูลของ PK WMS หรือซิงค์ข้ามเครื่อง QR ป้ายพาเลตที่พิมพ์จากเครื่องหนึ่งจะค้นทะเบียนไม่พบในอีกเครื่องหนึ่ง จึงควรใช้เครื่องเดิมตลอดกระบวนการและสำรองข้อมูลตามนโยบายงานคลัง การยืนยันจัดเก็บอัปเดตผังคลังเท่านั้น ไม่แก้ Stock RM Excel/ERP อัตโนมัติ
